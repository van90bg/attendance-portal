# Hermes local cache (desktop-v2.db) — local-only, do not commit
.freebuff/
# build local (scripts/build-local.js) — temp, không commit
index.local.html
# codegraph cache (tool DB, local-only — không commit)
.codegraph/

# ===== Config / secrets (AGENTS.md rule 4 — không commit) =====
.clasp.json
.clasprc.json
*.clasprc.json
.env
